import 'package:flutter/material.dart';

class BottomSheetAudioPlayer extends StatelessWidget {
  const BottomSheetAudioPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
